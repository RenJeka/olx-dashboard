// Стильові константи розмірів/відступів для Chakra-пропсів — щоб керувати
// геометрією UI з одного місця, а не зашитими числами по компонентах.
// Значення — у нотації Chakra spacing/sizing scale (число = крок 0.25rem, рядок = токен).

/** Ширина бічної панелі «Пошуки» (Chakra sizing token, '80' = 20rem). */
export const SIDEBAR_WIDTH = '80';

/** Горизонтальний/нижній відступ контентної області таблиці. */
export const CONTENT_PAD_X = 4;
export const CONTENT_PAD_Y = 4;

/** Відступ блоків порожніх/завантажувальних станів (по центру області). */
export const EMPTY_STATE_PAD = 8;

/** Дефолтні розміри оверлеїв (узгоджені між схожими діалогами/дроверами). */
export const DIALOG_SIZE = {
  /** Прості форми/підтвердження. */
  form: 'md',
  /** Контент із великим текстом (опис, звіт скану). */
  wide: 'lg',
} as const;

export const DRAWER_SIZE = {
  /** Налаштування, фільтри. */
  default: 'sm',
} as const;
